let dataUser = prompt("Ingresa el número de iteraciones que desees");

for (let i = dataUser; i >= 1; i--) {
  document.write(i);
  5;
  for (let rep = i - 1; rep >= 1; rep--) {
    document.write(i);
  }
  document.write("<br>");
}
